<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Blogs</title>
    <!-- swiper slider -->
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.css"
    />
      <!-- font awesome cdn -->
      <link
        rel="stylesheet"
        href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css"
      />
      <!-- bootstrap -->
      <link
        href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
        rel="stylesheet"
      />
      <!-- custom css -->
      <link rel="stylesheet" href="../assets/css/style.css" />
  </head>
  <body>
    <% if (success_message.length > 0) { %>
    <div class="alert alert-success" id="flash"><%= success_message %></div>
    <% } %>

    <header class="logo-head">
      <div
        class="container d-flex justify-content-between align-items-center py-3"
      >
        <!-- Logo Section -->
        <div id="header"></div>

        <!-- Navigation Section -->
        <div id="header"></div>
      </div>
    </header>

    <section class="home-slider">
      <div class="swiper mySwiper"></div>
    </section>

    <section class="blogs">
      <center>
        <div class="container-blog">
          
          <div class="blog-content mt-4">
            <%  
             let newOne = data.sort((a, b) => new Date(b.date) - new
            Date(a.date)).slice(0, 5); newOne.map((user, index) => { %>
            <h3 name="title"><%= user.title %></h3>
            <div class="date mb-3">
              <%= new Date(user.date).toLocaleDateString('en-IN') %>
            </div>
            <img src="../<%= user.image %>" alt="" width="100%" height="auto" />
            <div class="description mt-4">
              <p id="short-desc-<%= index %>" class="short-desc">
                <%= user.description.slice(0, 200) %>...
              </p>
              <p
                id="full-desc-<%= index %>"
                class="full-desc"
                style="display: none"
              >
                <%= user.description %>
              </p>
              <button
                class="read-more-btn btn btn-link"
                onclick="toggleDescription(<%= index %>)"
              >
                Read More
              </button>
            </div>
            <% }) %>
          </div>
        </div>
      </center>
    </section>

    <section class="explore-section">
      <div class="container p-5">
        <div class="text-center">
          <h2>Finding Perfect Spot For Surfing</h2>
          <p class="text-muted">Discover places that inspire and amaze</p>
        </div>
        <div class="row mt-4 justify-content-center">
          <div class="col-md-3">
            <img
              src="../assets/images/slide-01-big-300x200.jpg"
              class="img-fluid rounded"
              alt="Destination 1"
            />
          </div>
          <div class="col-md-3">
            <img
              src="../assets/images/slide-02.jpg"
              class="img-fluid rounded"
              alt="Destination 2"
            />
          </div>
          <div class="col-md-3">
            <img
              src="../assets/images/slide-03-big-300x200.jpg"
              class="img-fluid rounded"
              alt="Destination 3"
            />
          </div>
        </div>
        <p class="mt-3 text-center text-muted">
          It may comfort you to know that Fry’s death took only fifteen seconds,
          yet the pain was so intense, that it felt to him like fifteen years.
        </p>
      </div>
    </section>

    <div class="separator-line">
      <div class="line"></div>
      <div class="social-icons">
        <a href="https://facebook.com" target="_blank">
          <i class="fab fa-facebook-f"></i>
        </a>
        <a href="https://twitter.com" target="_blank">
          <i class="fab fa-twitter"></i>
        </a>
        <a href="https://instagram.com" target="_blank">
          <i class="fab fa-instagram"></i>
        </a>
        <a href="https://linkedin.com" target="_blank">
          <i class="fab fa-linkedin-in"></i>
        </a>
      </div>
      <div class="line"></div>
    </div>

    <section class="about-section py-5">
      <div class="container">
        <div class="row">
          <!-- About Me Section -->
          <div class="col-md-3">
            <h5>About Me</h5>
            <div class="about-me">
              <img
                src="../assets/images/about-small.jpg"
                alt="About Me"
                width="80%"
                class="img-fluid mb-3"
              />
              <p>
                Hello. I am a freelance writer. I live in a small town somewhere
                in the world.
              </p>
            </div>
          </div>

          <!-- Tags Section -->
          <div class="col-md-3">
            <h5>Tags</h5>
            <div class="tags">
              <span class="tag">adaptive</span>
              <span class="tag">client</span>
              <span class="tag">css</span>
              <span class="tag">design</span>
              <span class="tag">html</span>
              <span class="tag">jungle</span>
              <span class="tag">layout</span>
              <span class="tag">mobile</span>
              <span class="tag">photo</span>
              <span class="tag">responsive</span>
              <span class="tag">sound</span>
              <span class="tag">think</span>
              <span class="tag">typography</span>
              <span class="tag">web design</span>
            </div>
          </div>

          <!-- Trending Section -->
          <div class="col-md-3">
            <h5>Latest</h5>
            <div class="trending">
              <% // Sort by date and take the latest 3 entries const
              trendingItems = data.sort((a, b) => new Date(b.date) - new
              Date(a.date)).slice(0, 2); trendingItems.map((item) => { %>
              <div
                class="trending-post mb-3 d-flex align-items-center justify-content-evenly"
              >
                <img
                  src="../<%= item.image %>"
                  alt="Trending Post"
                  height="auto"
                  width="40%"
                  class="img-fluid rounded mb-2 col-5"
                />
                <div class="fs-6 col-5">
                  <p class="p-0 m-0"><%= item.title %></p>
                  <small
                    ><%= new Date(item.date).toLocaleDateString('en-IN')
                    %></small
                  >
                </div>
              </div>
              <% }) %>
            </div>
          </div>

          <!-- Recent Posts Section -->
          <div class="col-md-3">
            <h5>Recent Posts</h5>
            <ul class="recent-posts">
              <% const recentItems = data.sort((a, b ) => new Date(b.date) - new
              Date(a.date)).slice(0, 7); recentItems.map((item) => { %>
              <li><%= item.title %></li>
              <% }) %>
            </ul>
          </div>
        </div>
      </div>
    </section>

    <!-- swiper slider -->
    <script src="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.js"></script>
    <!-- slider -->
    <script src="../assets/js/slider.js"></script>
    <!-- bootstrap -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <!-- minimal.js  -->
    <script src="../assets/js/minimal.js"></script>
    <!-- header.js -->
    <script src="../assets/js/header.js"></script>
  </body>
</html>
